"""
Auth strategy implementations — concrete adapters for :class:`AuthStrategy`.

Each sub-package (oauth2, api_key, …) exports a single strategy class that
implements the strategy port.  Internal mechanics are private.
"""
