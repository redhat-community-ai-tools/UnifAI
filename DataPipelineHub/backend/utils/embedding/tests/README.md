# Embedding Model – Test Suite

This folder contains basic test for different options to integrate with our embedding model (whether it's via models_corp directly or via open-source library)

## 🚀 How to Run a Test

From the backend root folder, use the following command structure:

```bash
 python -m utils.embedding.tests.embedding_model_test
```