apiVersion: v1
kind: Service
metadata:
  name: {{ include "slack-proxy.fullname" . }}
  labels:
    {{- include "slack-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "slack-proxy.selectorLabels" . | nindent 4 }}
